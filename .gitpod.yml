tasks:
  - name: Setup & Run
    init: |
      flutter config --enable-web
      cd vertex
      flutter pub get
    command: |
      cd vertex
      flutter run -d web-server --web-port=8080 --web-hostname=0.0.0.0

ports:
  - port: 8080
    onOpen: open-preview
